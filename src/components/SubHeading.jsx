import React from 'react';

const SubHeading = ({ text }) => (
    <div className="text-center">
        <div className="text-3xl font-bold text-gray-900 mb-10 mt-4">{text}</div>
    </div>
);

export default SubHeading;
